-- [[ WYVERN PRO - XENO & GLOBAL BYPASS ]]
-- Amaç: Adonis 267 Hatasını aşmak ve her özelliği gizli (bypass) çalıştırmak.

local Success, Error = pcall(function()
    -- 🛡️ [PHASE 1: UNIVERSAL BYPASS]
    local lp = game:GetService("Players").LocalPlayer
    
    -- Kick fonksiyonunu etkisizleştir (Xeno Uyumluluk Modu)
    if lp then
        local oldKick
        oldKick = hookfunction(lp.Kick, function(self, reason)
            warn("Wyvern: Kick engellendi! Sebep: " .. tostring(reason))
            return nil
        end)
    end

    -- Namecall Bypass (Adonis & AC sinyallerini susturur)
    local mt = getrawmetatable(game)
    local oldNC = mt.__namecall
    setreadonly(mt, false)

    mt.__namecall = newcclosure(function(self, ...)
        local method = getnamecallmethod()
        if not checkcaller() and (method == "FireServer" or method == "InvokeServer") then
            local rName = tostring(self)
            if rName:find("Adonis") or rName:find("Check") or rName:find("\0") or rName:find("Control") or rName:find("Log") then
                return nil -- Sinyali sunucuya gönderme, Adonis'i kör et.
            end
        end
        return oldNC(self, ...)
    end)
    setreadonly(mt, true)
end)

if not Success then warn("Bypass Hatası: " .. tostring(Error)) end

-- ⚙️ [PHASE 2: ELITE CONFIG]
local Wyvern = {
    Combat = {
        SilentAim = true,
        FOV = 180,
        AimKey = Enum.KeyCode.E,
        TargetPart = "Head"
    },
    Visuals = {
        ESP = true,
        Boxes = true,
        Names = true,
        TeamCheck = true,
        FOVCircle = true
    },
    Movement = {
        WalkSpeed = 35, -- Anti-Ban sınırında hız
        JumpPower = 60,
        Bhop = true,
        InfJump = false
    },
    MenuKey = Enum.KeyCode.RightShift
}

-- 🎯 [PHASE 3: TARGETING ENGINE]
local FOVCircle = Drawing.new("Circle")
FOVCircle.Color = Color3.fromRGB(0, 255, 255)
FOVCircle.Thickness = 1
FOVCircle.Filled = false

local function GetTarget()
    local Target, MaxDist = nil, Wyvern.Combat.FOV
    for _, v in pairs(game.Players:GetPlayers()) do
        if v ~= game.Players.LocalPlayer and v.Character and v.Character:FindFirstChild(Wyvern.Combat.TargetPart) then
            if not (Wyvern.Visuals.TeamCheck and v.Team == game.Players.LocalPlayer.Team) then
                local pos, vis = workspace.CurrentCamera:WorldToViewportPoint(v.Character[Wyvern.Combat.TargetPart].Position)
                if vis then
                    local mag = (Vector2.new(pos.X, pos.Y) - game:GetService("UserInputService"):GetMouseLocation()).Magnitude
                    if mag < MaxDist then 
                        MaxDist = mag 
                        Target = v.Character[Wyvern.Combat.TargetPart] 
                    end
                end
            end
        end
    end
    return Target
end

-- 🔄 [PHASE 4: MAIN LOOP - BYPASSED FEATURES]
game:GetService("RunService").RenderStepped:Connect(function()
    -- FOV Visual
    FOVCircle.Visible = Wyvern.Visuals.FOVCircle
    FOVCircle.Radius = Wyvern.Combat.FOV
    FOVCircle.Position = game:GetService("UserInputService"):GetMouseLocation()

    local Char = game.Players.LocalPlayer.Character
    local Hum = Char and Char:FindFirstChild("Humanoid")
    local HRP = Char and Char:FindFirstChild("HumanoidRootPart")
    
    if Hum and HRP then
        -- 🚀 Speed Bypass (CFrame Injection)
        if Wyvern.Movement.WalkSpeed > 16 then
            HRP.CFrame = HRP.CFrame + (Hum.MoveDirection * (Wyvern.Movement.WalkSpeed / 100))
        end
        -- 🐰 Bhop
        if Wyvern.Movement.Bhop and game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.Space) and Hum.FloorMaterial ~= Enum.Material.Air then
            Hum.Jump = true
        end
    end
end)

print("WYVERN XENO-ALPHA LOADED | BYPASS: SUPREME")
